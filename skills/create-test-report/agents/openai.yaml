interface:
  display_name: "テスト実行レポート作成"
  short_description: "テスト成果物と実行結果をHTMLレポートに固定する支援"
  default_prompt: "Use $create-test-report to snapshot current test artifacts and execution results into an HTML test report."
