interface:
  display_name: "テストケース作成"
  short_description: "行単独で実行できるテストケースをMarkdownと人間実行CSVで作成する支援"
  default_prompt: "Use $create-test-cases to create row-independent, execution-ready, independently reportable Markdown test case files plus a companion CSV for human-executed cases."
