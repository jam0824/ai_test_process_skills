interface:
  display_name: "テストケース作成"
  short_description: "テスト設計から実行区分別に4ファイルのMarkdownテストケースを作成する支援"
  default_prompt: "Use $create-test-cases to create separate traceable Markdown test case files for code-based, E2E automated, human-executed, and question-wait cases."
