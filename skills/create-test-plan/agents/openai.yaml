interface:
  display_name: "テスト計画書作成"
  short_description: "資料からテスト計画書を作成し、再実行時はリスクとアプローチを追加検討する支援"
  default_prompt: "Use $create-test-plan to draft or refine a Markdown test plan; on a re-run, further consider overlooked product risks and add traceable test approaches, use-case/scenario strategy, generic QA coverage, and downstream analysis/design depth expectations."
