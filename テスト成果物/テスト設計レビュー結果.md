# テスト設計レビュー結果

## 1. レビュー対象

- レビュー対象: `テスト成果物/テスト設計.md`
- 質問票: `テスト成果物/テスト設計_質問票.md`
- レビュー結果: `テスト成果物/テスト設計レビュー結果.md`

## 2. 参照資料

- `テスト成果物/テスト分析.md`
- `テスト成果物/テスト分析_質問票.md`
- `テスト成果物/テスト計画書.md`
- `spec/仕様書.md`
- `sample_app/README.md`
- `sample_app/retirement_simulator.html`
- `package.json`
- `playwright.config.js`

## 3. レビュー観点

- `TV001` から `TV042` の全観点が `TDxxx` に落ちているか。
- 各行が `TVxxx`、`TAxxx`、仕様、リスクIDへ追跡可能か。
- 仕様内の有限リスト、境界値、ブラウザ・viewport・入力属性のリストが全件消費または代表抽出理由つきで設計されているか。
- `ケース分解候補` が条件/入力やテストパターンへ反映され、後続ケース分割単位が曖昧に残っていないか。
- 実施方法が主実施方法として明確か。複数レーンが必要な場合は主/補助が分かるか。
- 期待結果が後続のテストケースで合否判定可能か。
- `状態`、`## 4. 質問待ち項目`、`テスト設計_質問票.md` の三点が整合しているか。
- 参照しているローカル成果物や既存テスト資産の存在確認が必要な設計が、不明確なまま設計済みになっていないか。

## 4. レビュー・修正サマリー

### 1回目レビュー

| 優先度 | 観点 | 問題 | 場所 | 影響 | 修正方針 |
|---|---|---|---|---|---|
| P1 | 仕様内リスト消費 | 所得税ブラケット境界のうち、仕様にある9,000,000円、18,000,000円、40,000,000円の境界が設計上見えていなかった | `TD002` | 高リスクの手取り計算で上位税率境界の不具合を後続ケース化できない | `TD002` に全ブラケット境界の直前・境界・直後を後続ケース分割単位として追記 |
| P1 | 質問整合性 | `DQ001` が存在するのに、`TD019` は `設計済み` で、質問待ち項目にも出ていなかった | `TD019`, `## 4. 質問待ち項目`, `テスト設計_質問票.md` | 期待結果未確定のまま実装・実行へ進む可能性がある | `TD019` を `質問待ち` にし、期待結果を `要確認` として質問待ち項目へ追加 |
| P2 | 圧縮条件 | 複数値や複数環境を含む行で、後続ケース分割単位が明示されていなかった | `TD001`, `TD003`, `TD006`, `TD008`, `TD009`, `TD011`, `TD014`, `TD015`, `TD016`, `TD017`, `TD018`, `TD024`, `TD025`, `TD030`, `TD040`, `TD041`, `TD042`, `TD043`, `TD045` | 後続テストケース作成時に1ケースへ圧縮される恐れがある | 条件/入力に後続ケース分割単位を明示 |
| P2 | 実施方法の明確性 | `AまたはB` の実施方法が残っていた | `TD020`, `TD021`, `TD026`, `TD029`, `TD035`, `TD042`, `TD043`, `TD044` | 実装レーンの判断がぶれ、未実装や重複実装が起きやすい | 主実施方法を明記し、補助手段は補助証跡として表現 |

対応:

- `TD002` に仕様上の全所得税ブラケット境界を反映した。
- `TD019` と `## 4. 質問待ち項目` を `DQ001` と整合させた。
- 複数値・複数環境を含む設計行に、後続ケース分割単位を明示した。
- 実施方法の `または` 表現を主/補助へ整理した。

### 2回目レビュー

`P0`、`P1`、`P2` の修正すべき指摘は残っていない。

確認結果:

- `TV001` から `TV042` は少なくとも1件の `TDxxx` でカバーされている。
- 所得税ブラケット境界、代表ブラウザ、代表viewport、入力属性、ランダムイベント制御条件は設計上の分割単位として見える。
- `TD019`、質問待ち項目、`DQ001` は相互参照できる。
- 実施方法は主実施方法が判別できる。
- `sample_app/test_retirement_simulator.html` は現時点の `sample_app` 配下には存在しないが、`TD020` は「正式回帰資産の存在確認」そのものを目的とする設計であるため、設計不備ではなく後続実行時に欠落issueとして扱う。

## 5. 最終レビュー結果

`P0`、`P1`、`P2` の修正すべき指摘は残っていない。

必要十分性の確認:

- 計算系の逃しバグとして、課税所得0円境界、全所得税ブラケット境界、丸め、負値・非有限値、投資補填上限を想定し、設計済み。
- 期間遷移・給与変更系の逃しバグとして、退職年齢/年金開始年齢境界、年齢逆転、給与変更の有効条件、開始年齢前/同年/退職後、同一年齢優先を想定し、設計済み。
- ランダムイベント系の逃しバグとして、発生率0/100%、乱数境界、イベント選択先頭/末尾を想定し、設計済み。
- UI/非機能の逃しバグとして、入力属性不一致、XSS、外部通信、永続化、代表ブラウザ差異、性能劣化、注意表示不足、アクセシビリティ基本操作を想定し、設計済み。
- 質問待ちは `TD019` / `DQ001` の1件で、仕様前提外条件の期待結果が未確定であることを明示した。

## 6. 残課題

| 優先度 | 内容 | 推奨対応 |
|---|---|---|
| P3 | 既存の後続テストケースは、今回拡張した `TD002` の上位所得税ブラケット境界をまだ全件ケース化していない可能性がある | 次にテストケース作成・レビューを再実行し、`TD002` の全境界分割が反映されているか確認する |
